"""Dev entrypoint: `uv run python -m agent` / `make run-agent`.

TODO: build the LangChain agent graph, connect to the mcp-server MCP endpoint, and start a
chat loop (or serve it behind an API for web/ to call).
"""


def main() -> None:
    print("agent: no implementation yet — see AGENTS.md")


if __name__ == "__main__":
    main()
