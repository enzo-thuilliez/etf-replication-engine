[workspace]
resolver = "2"
members = ["engine", "mcp-server"]

[workspace.package]
version = "0.1.0"
edition = "2021"
license = "MIT"
# repository = "https://github.com/<user>/etf-replication-engine" # TODO: set once published

# Shared dependency versions go here as the workspace grows, e.g.:
# [workspace.dependencies]
# serde = { version = "1", features = ["derive"] }
