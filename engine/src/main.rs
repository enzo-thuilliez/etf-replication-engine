//! Dev/debug entrypoint for the engine crate (`make run-engine`).
//!
//! TODO: wire this up to a small CLI (e.g. run `optimize_basket` against a sample dataset in
//! `data/`) once the engine has logic to exercise.

fn main() {
    println!("engine: no implementation yet — see PROJECT.md and AGENTS.md");
}
