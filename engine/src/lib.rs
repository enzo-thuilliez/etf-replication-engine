//! Core optimization engine for index replication and ETF basket rebalancing.
//!
//! TODO: implement basket optimization (`optimize_basket`), tracking-error computation
//! (`compute_tracking_error`), and rebalance simulation (`simulate_rebalance`), per the
//! contract in `AGENTS.md`. See `PROJECT.md` for the domain problem this crate solves.
