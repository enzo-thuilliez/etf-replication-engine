# Agent evaluation

TODO: evaluation harness for the LangChain agent (tool-calling correctness, response quality),
using `deepeval` and/or `ragas`. Phase 2+ of the roadmap — requires `src/agent/` and
`mcp-server/` to exist first. Evaluation criteria should be derived from the contract in
[AGENTS.md](../AGENTS.md).
