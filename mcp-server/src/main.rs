//! MCP server + axum API entrypoint.
//!
//! TODO: expose `optimize_basket`, `compute_tracking_error`, and `simulate_rebalance` as MCP
//! tools via `rmcp` (contract: see `AGENTS.md`), and serve the same `engine` crate over an axum
//! HTTP API for the web dashboard (`web/`).

fn main() {
    todo!("mcp-server not yet implemented — see AGENTS.md for the tool contract");
}
