# Notebooks

Jupyter notebooks for exploration and prototyping (data exploration, optimization model
sketches, agent prompt iteration). Not production code — don't import from here into `engine/`,
`mcp-server/`, or `src/agent/`.

No notebooks yet.
