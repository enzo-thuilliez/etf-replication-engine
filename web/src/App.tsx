// TODO: replace this placeholder with the dashboard layout (basket table, tracking-error
// chart, rebalance cost panel) and an agent chat panel once the MCP server + API exist.
export default function App() {
  return (
    <main>
      <h1>ETF Replication Engine</h1>
      <p>Dashboard — coming soon.</p>
    </main>
  );
}
